import React from "react"
import Navbar from "./Navbar"

export default function LandingPage() {
    return (
        <div>
            <Navbar></Navbar>
            <h1>Test Landing (In progress)</h1>
            <button>Login to Spotify</button>
        </div>
    )
}